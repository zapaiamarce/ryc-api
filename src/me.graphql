type UserPayload {
  id: String,
  email: String,
  fullName: String
  bio: String
  deliveryCenterLocation: Json
  deliveryRadiusInMeters: Int
  deliveryCenterAddress: String
}

extend type Query {
  me: UserPayload
}
