subscription {
  Food(filter: {
    mutation_in: [CREATED, UPDATED, DELETED]
  }) {
    mutation
    previousValues {
      id
    }
    node {
      id
      addressstr
      title
      dates{
        delivery
        orderDeadline
      }
      cook{
        id
        deliveryCenterLocation
        deliveryRadiusInMeters
      }
    }
  }
}