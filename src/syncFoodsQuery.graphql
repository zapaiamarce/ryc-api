subscription {
  Food(filter: {
    mutation_in: [CREATED, UPDATED, DELETED]
  }) {
    mutation
    previousValues {
      id
    }
    node {
      id
      title
      dates{
        delivery
        orderDeadline
      }
      cook{
        id
        deliveryCenterLocation
        deliveryRadiusInMeters
      }
    }
  }
}